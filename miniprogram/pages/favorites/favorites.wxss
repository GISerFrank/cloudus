.container { padding: 30rpx; }
.page-title { font-size: 48rpx; font-weight: bold; margin-bottom: 30rpx; }
.works-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 15rpx;
}
.grid-item {
  width: 100%;
  height: 220rpx;
  border-radius: 12rpx;
  background-color: #eee;
}